
html
    head
        title='Airbnb'
        link(rel='stylesheet', href='/css/index.css')
    body
        nav.nav
            div.nav-logo
                img(src="/static/logo2.svg", alt="logo")
            div.language
            div.user
        ariticle.reservation
            ul.tab
                li 숙소
                li 체험
                li 온라인 체험
            section.search
                div.search-input
                    label(for="location") 위치
                    input(type="text" name="location" placeholder="어디로 여행가세요?")
                div.search-input
                    label(for="checkin") 체크인
                    input(type="text" name="checkin" placeholder="날짜 추가")
                div.search-input
                    label(for="checkout") 체크아웃
                    input(type="text" name="checkout" placeholder="날짜 추가")
                div.search-input
                    label(for="personnel") 인원
                    input(type="text" name="personnel" placeholder="게스트 추가")
                button.search-input 검색

        
